import styled from "styled-components";
import { ConfigLayout } from "../../styles/configLayout";

export const Root = styled.div`
  margin-top: ${ConfigLayout.navBarHeight}px;

  > section {
    width: 100%;
  }
`